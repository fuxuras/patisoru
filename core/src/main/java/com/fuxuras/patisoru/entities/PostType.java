package com.fuxuras.patisoru.entities;

public enum PostType {
    USER_POST,      // Standard user post
    VET_POST,       // Post by a user with VET role
    VERIFIED_POST   // Post by a user with a special verified role
}